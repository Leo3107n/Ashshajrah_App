/** Coordinator fee voucher batch route. */
import CoordinatorVouchers from "../../../src/features/coordinator/CoordinatorVouchers";

export default function Vouchers() {
  return <CoordinatorVouchers />;
}
