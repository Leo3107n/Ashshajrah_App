/** Coordinator Students & Parents directory route. */
import CoordinatorPeople from "../../../src/features/coordinator/CoordinatorPeople";

export default function People() {
  return <CoordinatorPeople />;
}
