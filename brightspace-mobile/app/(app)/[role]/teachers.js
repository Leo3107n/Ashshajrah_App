/** Coordinator Teachers & assignments route. */
import CoordinatorTeachers from "../../../src/features/coordinator/CoordinatorTeachers";

export default function Teachers() {
  return <CoordinatorTeachers />;
}
